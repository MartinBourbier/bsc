use std::str::FromStr;
use bsc_common::symbol::Symbol;

grammar;

pub Term: i32 = {
    <n:Num> => n,
    "(" <t:Term> ")" => t,
};

Num: i32 = r"[0-9]+" => i32::from_str(<>).unwrap();

pub Id: Symbol = r"[a-zA-Z_][a-zA-Z0-9_]*" => Symbol::new(<>);
